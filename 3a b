3a:
git clone -b main https://github.com/Binduraj-16/To-Do
cd To-Do
ls
cat bin.txt
touch text1.txt
git add .
git commit -m "new file commit"
git pull origin main --rebase
git push origin main
# (If authentication required → use GitHub Personal Access Token as password)
git checkout -b slave
touch file2.txt
git add .
git commit -m "work in slave branch"
git fetch origin
git rebase origin/main
git checkout main
git merge slave
git push origin main
git checkout slave
ls
3b:
git clone https://github.com/Binduraj-16/To-Do
cd To-Do
gedit prgm.txt
# (write something like: "This is main branch code" and save)
git add prgm.txt
git commit -m "prgm in main"
git checkout -b feature-branch
gedit prgm.txt
# (change content: "This is feature branch code" and save)
git add prgm.txt
git commit -m "prgm in feature-branch"
git checkout main
git merge feature-branch (if no conflict then git push origin main)
CONFLICT (content): Merge conflict in prgm.txt
# 9. Open file to fix conflict
gedit prgm.txt
<<<<<<< HEAD
This is main branch code
=======
This is feature branch code
>>>>>>> feature-branch
remove <<<<<<< ======= >>>>>>>
git add prgm.txt
git commit -m "resolved merge conflict"
git push origin main
